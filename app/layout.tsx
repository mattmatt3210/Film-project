import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "CinemaVault - Blockchain Movie Rentals",
  description: "Your premium destination for blockchain-powered movie rentals with Web3 technology",
  keywords: "movies, blockchain, ethereum, web3, streaming, rental",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
